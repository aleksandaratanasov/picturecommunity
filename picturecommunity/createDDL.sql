CREATE TABLE my_user (ID BIGINT NOT NULL, PASSWORD VARCHAR(255), UPLOADS BIGINT, USERNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE my_image (ID BIGINT NOT NULL, COMMENT VARCHAR(255), NAME VARCHAR(255), PATH VARCHAR(255), UPLOADTIME BYTEA, VIEWSTATUS BOOLEAN, UPLOADER_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE my_user_my_user (User_ID BIGINT NOT NULL, contacts_ID BIGINT NOT NULL, PRIMARY KEY (User_ID, contacts_ID))
ALTER TABLE my_image ADD CONSTRAINT FK_my_image_UPLOADER_ID FOREIGN KEY (UPLOADER_ID) REFERENCES my_user (ID)
ALTER TABLE my_user_my_user ADD CONSTRAINT FK_my_user_my_user_User_ID FOREIGN KEY (User_ID) REFERENCES my_user (ID)
ALTER TABLE my_user_my_user ADD CONSTRAINT FK_my_user_my_user_contacts_ID FOREIGN KEY (contacts_ID) REFERENCES my_user (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
